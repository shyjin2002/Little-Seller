import 'package:flutter/material.dart';

class OrderHistorypage extends StatelessWidget {
  const OrderHistorypage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Order history Screen'));
  }
}